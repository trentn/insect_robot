/*
 * Implementation of kinematics.h
 */

#include "kinematics.h"
#include <math.h>
#include <stdlib.h>

struct point* fwd_leg_kin(struct pose* pose) {
    struct pose* out_pose = (struct pose*)malloc(sizeof(struct pose));

        

    return out_pose;
}
